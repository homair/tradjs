extend _layout

block content
  .container
      .row.clearfix
        .col-md-12.table-responsive
          table#tab_logic.table.table-bordered.table-hover.table-sortable
            thead.color
              tr
                td.
                 Clés
                each val in listeLang
                  td= val.label

                tbody
                each val, key in objetFinale.fr
                  tr(id='row_'+ key.replace(/\./gi, '-'))
                    td.col-md-12
                      span(id='data.'+key)=key
                      button.close(id='btn_'+key, type='button', value='Supprimer', data-inputkey=key, data-dismiss='alert')
                        span.cross(aria-hidden='true') x
                        span.sr-only Close

                      each lang, keyLang in listeLang
                        td
                          input.form-control(id=keyLang, type='text', value=objetFinale[keyLang][key], data-key='data.'+key, data-lang=keyLang)


